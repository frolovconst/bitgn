#!/usr/bin/env bash
set -euo pipefail

repo_root="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cd "$repo_root"

export BITGN_BENCHMARK_KIND="sandbox"

exec uv run python -m benchmark.bitgn.cli "$@"
